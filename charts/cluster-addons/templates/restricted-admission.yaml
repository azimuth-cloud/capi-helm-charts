{{- if .Values.restrictedAdmission.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cluster-addons.componentName" (list . "restricted-admission") }}-config
  labels:
    {{- include "cluster-addons.componentLabels" (list . "restricted-admission") | nindent 4 }}
    addons.stackhpc.com/watch: ""
stringData:
  overrides: |
    {{- toYaml .Values.restrictedAdmission.release.values | nindent 4 }}
---
apiVersion: addons.stackhpc.com/v1alpha1
kind: HelmRelease
metadata:
  name: {{ include "cluster-addons.componentName" (list . "restricted-admission") }}
  labels: {{ include "cluster-addons.componentLabels" (list . "restricted-admission") | nindent 4 }}
  annotations:
    # Tell Argo to ignore the non-controller owner references for this object
    argocd.argoproj.io/sync-options: "ControllerReferencesOnly=true"
spec:
  clusterName: {{ include "cluster-addons.clusterName" . }}
  bootstrap: true
  chart:
    repo: {{ .Values.restrictedAdmission.chart.repo }}
    name: {{ .Values.restrictedAdmission.chart.name }}
    version: {{ default .Chart.Version .Values.restrictedAdmission.chart.version }}
  targetNamespace: {{ .Values.restrictedAdmission.release.namespace }}
  releaseName: restricted-admission
  valuesSources:
    - secret:
        name: {{ include "cluster-addons.componentName" (list . "restricted-admission") }}-config
        key: overrides
{{- end }}