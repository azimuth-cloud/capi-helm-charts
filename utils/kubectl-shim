#!/bin/sh

set -eo pipefail

#####
# Simple script that selects the correct kubectl version for the target server
#####

server_version="$(kubectl-$KUBECTL_VN_LATEST version -o json | jq -r '"v" + .serverVersion.major + "." + .serverVersion.minor')"
exec kubectl-$server_version "$@"
